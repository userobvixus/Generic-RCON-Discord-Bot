<!DOCTYPE html>
<html lang="fr" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion Requise</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #121417; display: flex; align-items: center; justify-content: center; height: 100vh; }
        .card { background-color: #1e2126; border: 1px solid #2f333a; width: 100%; max-width: 400px; }
        h4 { color: #ececec; }
        label { color: #aeb0b7; }
    </style>
</head>
<body>
    <div class="card p-4 shadow-lg">
        <div class="card-body text-center">
            <h4 class="mb-3">🔒 Accès Sécurisé</h4>
            <p class="text-muted small mb-4">Nouvelle adresse IP détectée.<br>Veuillez entrer le code d'accès.</p>
            
            <% if (error) { %>
                <div class="alert alert-danger py-2 small"><%= error %></div>
            <% } %>

            <form action="/login" method="POST">
                <div class="mb-3 text-start">
                    <label class="form-label">Mot de passe Admin</label>
                    <input type="password" name="password" class="form-control text-center" required autofocus>
                </div>
                <button type="submit" class="btn btn-primary w-100">Déverrouiller</button>
            </form>
        </div>
    </div>
</body>
</html>